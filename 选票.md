## 🎯 选题背景

传统的电影购票 APP 流程繁琐：**搜电影 → 选影院 → 挑场次 → 看座位图 → 下单**。用户需要在多个页面间跳转，反复筛选。
 而在大模型时代，用户更习惯**“一句话表达需求”**。

**挑战在于**：
 如何构建一个**懂业务、会规划、能决策**的 AI Agent，让它不仅能“听懂”用户模糊的指令（如“周末想看个喜剧”），还能**自动规划执行路径**（先查热映喜剧 → 再找周末排片 → 自动优选座位），并在信息缺失时**智能追问**，在信息溢出时**自动跳步**？

**你的任务**：
 构建一个**“电影票智能体”系统**，通过**纯文本对话 + 动态卡片**的形式，重构购票全流程：

- **智能对话引擎**：实现**意图识别、槽位提取、多轮对话管理**。核心是**“自动跳步”逻辑**（用户一次性说完所有条件，直接出票）。
- **动态卡片交互**：对话过程中，Agent 能根据上下文动态推送“影片卡片”、“座位图卡片”、“订单确认卡片”。
- **业务决策策略**：在座位冲突、场次售罄、价格敏感等场景下，Agent 需给出**有温度的备选方案**，而不仅仅是报错。
- **开放创新**：鼓励学生设计独特的**推荐算法**、**个性化记忆**或**异常处理机制**。

**强制架构要求**：

1. **交互形式**：采用 **“对话窗口（Text） + 动态卡片（UI）”** 的双模交互。**无需实现真实语音**。
2. **核心逻辑**：必须实现**“任务规划与自动跳步”**。即：根据用户输入的槽位完备度，动态决定是“继续追问”还是“直接执行下一步”。
3. **业务闭环**：必须跑通“意图识别 → 选座/锁座 → 下单/支付（模拟） → 出票”全流程。
4. **上下文记忆**：支持多轮对话中的指代消解（如“便宜的”、“换一家”、“还是老位置”）。

## 📐 产品架构要求（简化版）

```plain
┌─────────────────────────────────────────────────────────────┐
│                  用户交互层 (Text + Cards)                  │
│  ┌───────────────────┐  ┌──────────────────────────────┐   │
│  │  💬 对话输入框     │  │  📱 动态卡片展示区            │   │
│  │  - 模拟语音文本输入 │  │  - 影片/影院/场次/座位卡片    │   │
│  │  - 历史记录展示   │  │  - 订单确认/支付模拟卡片      │   │
│  │  - 快捷建议 Chips │  │  - 推荐方案/异常提示卡片      │   │
│  └───────────────────┘  └──────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                           ↕ 事件驱动
┌─────────────────────────────────────────────────────────────┐
│                  电影票智能体引擎 (Agent Core)              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  意图与槽位  │  │  任务规划器  │  │  上下文记忆  │      │
│  │  [NLP 层]    │  │  [Planning]  │  │  [Memory]    │      │
│  │  - 意图分类  │  │  - 步骤拆解  │  │  - 槽位缓存  │      │
│  │  - 实体提取  │  │  - ★自动跳步 ★│  │  - 用户偏好  │      │
│  │  - 缺槽追问  │  │  - 策略推荐  │  │  - 指代消解  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              🛠️ 业务工具集 (Tools)                   │    │
│  │  - searchMovies(), selectCinema(), lockSeat()...    │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                           ↕ API 调用
┌─────────────────────────────────────────────────────────────┐
│                  票务业务中台 (Mock/Real)                   │
│  - 影片/影院/场次数据  - 座位库存与锁座  - 订单与支付模拟    │
└─────────────────────────────────────────────────────────────┘
```

## 📋 核心功能参考

### 1. 智能对话与任务规划

**A. 意图识别与槽位提取**

- 多槽位提取

  ：能从一句话中提取多个关键信息。

  - 输入：“帮我订两张明天下午离公司最近的 IMAX《流浪地球 3》”

  - 提取：

    {film: "流浪地球 3", time: "明天下午", location: "公司附近", hall: "IMAX", count: 2}

- **模糊意图处理**：用户说“周末想看个喜剧”，Agent 能识别意图为“选影片”，并基于“喜剧”、“周末”进行推荐。

**B. 任务规划与自动跳步**

- 动态执行流

  ：

  - **情况 1（信息缺失）**：用户只说“想看流浪地球”，Agent 追问：“您想去哪家影院？或者我为您推荐附近的？”
  - **情况 2（信息完备/溢出）**：用户一句话包含了电影 + 影院 + 时间 → Agent **自动跳过**选片、选影院步骤，直接生成“场次列表”或“选座卡片”。
  - **情况 3（上下文修正）**：用户在选座时说“太贵了，换便宜点的”，Agent 能理解这是对“场次/座位”的修正，自动重新筛选。

- **开放策略**：学生可自定义**推荐策略**（如：优先推荐价格低的？还是评分高的？还是座位最好的？）。

**C. 上下文记忆与指代消解**

- **多轮记忆**：用户说“换成第 2 个”，Agent 需知道是指“第 2 个影院”还是“第 2 个场次”（基于当前对话状态）。
- **偏好记忆**：用户说“还是老样子”，Agent 能调取历史订单中的偏好（如“ always 选 5 排中间”）。

### 2. 动态卡片交互

**A. 对话 + 卡片联动**

- **文本驱动 UI**：Agent 回复“为您找到以下影片”时，下方自动推送**影片列表卡片**（含海报、评分、按钮）。
- **UI 反哺文本**：用户点击卡片上的“查看座位”，Agent 自动接续对话：“好的，正在为您加载座位图...”并推送**座位卡片**。
- **可视化反馈**：在对话流中展示“当前进度条”（如：选片✅ → 选影院✅ → 选座🔄）。

**B. 异常与推荐卡片**

- **智能推荐**：当前场次售罄时，不直接报错，而是推送“推荐卡片”：“该场次已满，为您推荐相邻的 5 排 6 座，或 20 分钟后的下一场，您看哪个？”

### 3. 票务业务中台

**A. 核心业务能力**

- **数据查询**：支持影片、影院、场次、座位的多条件筛选。
- **锁座与订单**：实现座位锁定（防止超卖）、订单生成、模拟支付（点击按钮即视为支付成功）。
- **超时取消**：订单 15 分钟未支付，自动释放座位（可用定时任务或延迟队列模拟）。

**B. 个性化数据（Mock）**

- **用户画像**：预置一些用户偏好数据（如“用户 A 喜欢 IMAX”，“用户 B 只买 30 元以下的票”），供 Agent 调用。

### 4. 开放创新点

*此部分完全开放，学生可任选其一或自创：*

- ******极致推荐算法**：基于协同过滤或规则引擎，实现“比用户更懂用户”的座位/影片推荐。
- ******情感化交互**：Agent 回复带有情感色彩（如抢到高难度座位时：“太棒了！这是全场仅剩的黄金座！”）。
- ******智能谈判助手**：当票价过高时，Agent 自动检索优惠券或建议“拼单购买”（模拟）。
- **可视化行程规划**：购票成功后，自动生成“观影行程单”（含出发时间建议、周边美食推荐）。

## ⏱️ 工期

**MVP 范围**：

- **交互**：实现基础的“一问一答”文本对话，支持手动点击卡片跳转。
- **逻辑**：实现**顺序执行**流程（选片→选影院→选座），暂不做强跳步逻辑。
- **后端**：提供基础数据接口，锁座可用简单数据库事务。
- **目标**：跑通“对话买票”流程，无严重 Bug。

### 交付时间

需小组成员自行商讨，给出交付排期，制作交付甘特图。

## 🏆 重点关注

| 维度           | 评分关键点                                                 |
| -------------- | ---------------------------------------------------------- |
| **对话智能度** | 意图识别是否准确？**自动跳步**是否流畅？多轮追问是否自然？ |
| **策略与推荐** | 在信息缺失或冲突时，Agent 的**推荐方案**是否合理且有创意？ |
| **交互体验**   | 对话与卡片的联动是否丝滑？是否有可视化的进度反馈？         |
| **业务闭环**   | 选座→锁座→支付→出票 是否全链路打通？数据是否一致？         |
| **创新亮点**   | 是否有独特的情感交互、个性化记忆或行程规划等亮点？         |

## 💡 Tips

1. **状态机是核心**：画好你的对话状态机（State Machine）。明确每个节点需要哪些槽位，哪些槽位已满，哪些还缺。
2. **跳步逻辑是加分项**：重点设计“槽位检查器”。每次用户输入后，检查是否满足直接跳到下一步的条件。这是区分“普通 Chatbot”和“智能 Agent”的关键。
3. **Mock 数据要丰富**：预置一些有趣的影片、影院和座位数据，让演示更生动（如“情侣座”、“黄金区域”）。
4. **不要为了 AI 而 AI**：如果用户已经选好了座位，就不要强行让 AI 再问一遍。AI 的目的是**提效**，不是**增加步骤**。
5. **错误处理要温柔**：当座位被抢时，不要只说“失败”，要说“手慢了！不过旁边这两个连座也很棒，要不要试试？”